# 部署前最终检查清单

## ✅ 文件结构检查

- [x] `app/` 文件夹存在（Next.js核心）
- [x] `components/` 文件夹存在
- [x] `public/` 文件夹存在
- [x] `public/images/` 包含5张压缩后的JPG图片
- [x] `public/preview.html` 存在（用于部署）
- [x] 根目录 `preview.html` 存在（用于本地预览）

## ✅ 图片路径检查

### 本地预览（根目录 preview.html）
- [x] 使用相对路径：`public/images/xxx.jpg`
- [x] 本地打开时能正确显示

### 部署版本（public/preview.html）
- [x] 使用绝对路径：`/images/xxx.jpg`
- [x] 部署后访问 `/preview.html` 时能正确显示

## ✅ 图片文件检查

- [x] `premium.jpg` - 约887KB
- [x] `classic.jpg` - 约981KB
- [x] `organic.jpg` - 约1087KB
- [x] `spring.jpg` - 约896KB
- [x] `aged.jpg` - 约1102KB
- [x] 总大小约5MB，可以上传到GitHub

## ✅ 部署配置检查

- [x] `package.json` 配置正确
- [x] `next.config.js` 配置正确
- [x] `vercel.json` 配置正确
- [x] `.gitignore` 不忽略 `public/` 文件夹

## 📝 部署步骤

1. **上传到GitHub**（确保包含所有文件夹）：
   ```bash
   git add .
   git commit -m "准备部署：包含所有文件和压缩后的图片"
   git push origin main
   ```

2. **Vercel自动部署**：
   - Vercel会自动检测到更改
   - 运行 `npm install` 安装依赖
   - 运行 `npm run build` 构建项目
   - 部署到生产环境

3. **访问网站**：
   - Next.js应用：`https://your-domain.vercel.app/zh`
   - 静态预览页：`https://your-domain.vercel.app/preview.html`
   - 图片路径：`https://your-domain.vercel.app/images/xxx.jpg`

## ⚠️ 重要提示

- **本地预览**：打开根目录的 `preview.html`，图片路径是 `public/images/xxx.jpg`
- **部署后**：访问 `/preview.html`，图片路径是 `/images/xxx.jpg`（Vercel会自动服务public文件夹）
- **两个文件路径不同是正常的**，因为本地和部署环境不同

## 🎯 验证部署成功

部署后检查：
1. ✅ 访问 `/preview.html` 能看到页面
2. ✅ 5张产品图片正常显示
3. ✅ 点击"查看"按钮，详情页图片正常显示
4. ✅ 所有链接正常工作

