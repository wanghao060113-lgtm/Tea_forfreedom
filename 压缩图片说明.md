# 图片压缩说明

## 当前问题
- 每张PNG图片约3-4MB，太大
- 5张图片 × 2个位置 = 10张图片，占用约40MB
- GitHub上传限制导致无法提交

## 解决方案

### 方案1：使用在线工具压缩（最简单）

1. **访问图片压缩网站**：
   - https://tinypng.com/ （推荐，支持PNG和WebP）
   - https://squoosh.app/ （Google出品，功能强大）
   - https://compressor.io/

2. **压缩步骤**：
   - 上传 `public/images/` 文件夹中的5张PNG图片
   - 压缩后下载
   - 替换原来的图片文件

3. **目标大小**：
   - 每张图片压缩到 **200-500KB** 左右
   - 保持800x800px尺寸
   - 质量设置为80-85%即可

### 方案2：转换为WebP格式（推荐，更小）

WebP格式比PNG小50-70%，且质量更好：

1. **使用在线转换工具**：
   - https://cloudconvert.com/png-to-webp
   - https://convertio.co/zh/png-webp/

2. **转换步骤**：
   - 上传5张PNG图片
   - 转换为WebP格式
   - 下载后替换 `public/images/` 中的文件
   - 文件名改为：`premium.webp`、`classic.webp` 等

3. **更新HTML文件中的图片路径**：
   - 将 `.png` 改为 `.webp`
   - 例如：`/images/premium.png` → `/images/premium.webp`

### 方案3：使用命令行工具（如果你有ImageMagick）

```powershell
# 安装ImageMagick后，压缩图片
magick public\images\premium.png -quality 85 -resize 800x800 public\images\premium_compressed.png
```

## 推荐操作流程

1. ✅ **已完成**：删除根目录的 `images/` 文件夹（已节省约20MB）

2. **下一步**：压缩 `public/images/` 中的5张图片
   - 使用 https://tinypng.com/
   - 每张压缩到200-500KB
   - 或转换为WebP格式

3. **压缩后**：
   - 总大小应该从40MB降到5-10MB
   - 就可以正常上传到GitHub了

## 压缩后的预期大小

- 5张图片 × 300KB = 约1.5MB
- 其他文件 = 约1-2MB
- **总计约3-5MB** ✅ 可以轻松上传

